## КТ-1 (Тиждень 8)
- Loom: [посилання](https://www.loom.com/share/a5c0a4ae81934e42af83870d359bb360)
- Що зроблено: створено план в README, ініціалізовано масив дошки, назви процедур, спільні константи, таблиці руху фігур та символів (псевдографіки), налаштовано скелети модулів UI (відеопам'ять B800h) та ігрової логіки.

## КТ-2 (Тиждень 9)
- Loom: [посилання frontend](https://www.loom.com/share/d1d8f6a57a2d44849ba15256014ad3df)
- Loom: [посилання backend](https://www.loom.com/share/2833ef6a538f43159aa1febbab511424) 
- Що зроблено: розроблено кастомну графіку фігур через текстовий режим, реалізовано кольоровий курсор із підсвічуванням клітинок, написано повноцінний інтерактивний ігровий цикл із керуванням клавіатурою (стрілки для руху, Enter для вибору) та логікою меж дошки, а графіку успішно інтегровано з ігровою логікою для переміщення фігур, налаштовано візуалізацію пропорційного ігрового поля з правильним чергуванням кольорів і шаховою нотацією, реалізовано коректну обробку кольорів фігур за допомогою побітових масок, у game.asm оголошено всі основні процедури логіки гри (init_board, get_legal_moves, execute_move, is_square_attacked, is_in_check, is_checkmate), створено ai.asm зі стабом ai_turn, реалізовано ініціалізацію стартової позиції 32 фігур (init_board), реалізовано генерацію ходів для коня (8 L-подібних ходів, перевірка меж дошки та кольору фігури) та генерацію ходів для короля (8 напрямків на 1 клітинку, перевірка меж та кольору фігури), реалізовано запис згенерованих ходів у move_list, прописано процедуру execute_move для переміщення фігури та обробки захоплення, додано тестування генерації ходів через INT 21h у test.asm з виводом списку ходів у DOSBOX.